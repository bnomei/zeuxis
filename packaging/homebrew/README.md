# Homebrew Template

This folder contains a Homebrew formula template for `zeuxis`.

It is intended to be copied into a dedicated tap repository (for example `homebrew-zeuxis`) under:

`Formula/zeuxis.rb`

After each release, replace version/checksums in that tap formula using:

`scripts/homebrew-update-sha256.sh`
